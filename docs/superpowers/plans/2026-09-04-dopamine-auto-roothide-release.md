# DopamineAuto RootHide 2.4.9 Release Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Publish the RootHide DopamineAuto 2.4.9 source on `xct9m7gd9z/DopamineAuto`, run reproducible macOS tests and compilation, and produce a verified `DopamineAuto.tipa` Artifact.

**Architecture:** Preserve the RootHide source tree and its bundle identity. Replace the upstream workflow's self-cloning build with the reproducible checkout-based workflow used by the previous release, while keeping the RootHide-specific source-contract tests and build output name.

**Tech Stack:** GitHub Actions, macOS 15, Xcode 16.4, THEOS, iPhoneOS 16.5 SDK, Procursus, Python `unittest`, GNU Make, `unzip`.

---

### Task 1: Add the reproducible RootHide workflow

**Files:**
- Modify: `.github/workflows/roothide.yml`
- Create: `Tests/test_ci_workflow.py`

- [ ] **Step 1: Replace the workflow checkout and unpinned dependencies.**
  Use `actions/checkout` with recursive submodules, pin Xcode to 16.4, pin THEOS/trustcache/SDK revisions, run source-contract tests, download verified RootHide bootstraps if present, build `Application/Dopamine.tipa`, rename it to `DopamineAuto.tipa`, verify the ZIP and upload an Artifact retained for 14 days.

- [ ] **Step 2: Add workflow contract tests.**
  Assert that the workflow checks out the current commit, uses `macos-15`, selects Xcode 16.4, runs Python tests, verifies the Artifact, and uploads `DopamineAuto.tipa` with a SHA-based name.

- [ ] **Step 3: Run the local source-contract tests.**
  Run `python -m unittest discover -s Tests -p "test_*.py" -v` and require exit code 0.

### Task 2: Commit and push the RootHide branch

**Files:**
- Modify: Git remote configuration only; preserve the upstream remote under `upstream` and add `https://github.com/xct9m7gd9z/DopamineAuto.git` as `origin`.

- [ ] **Step 1: Review the diff and repository status.**
  Confirm only the workflow, CI test, and plan are new changes plus any required pinned bootstrap files.

- [ ] **Step 2: Commit the release automation.**
  Create a commit on `dopamine-auto-roothide` with message `ci: add reproducible RootHide 2.4.9 build`.

- [ ] **Step 3: Push the branch.**
  Push `dopamine-auto-roothide` to `xct9m7gd9z/DopamineAuto` and record the commit SHA.

### Task 3: Verify GitHub Actions output

**Files:**
- Artifact generated by GitHub Actions: `Application/DopamineAuto.tipa`

- [ ] **Step 1: Wait for the dispatched push workflow to complete.**
  Query the run by branch and commit until status is `completed`; do not report it as running after completion.

- [ ] **Step 2: Verify all job steps succeeded.**
  Confirm source tests, dependency installation, RootHide build, ZIP validation, and Artifact upload all have conclusion `success`.

- [ ] **Step 3: Verify the Artifact metadata.**
  Confirm the Artifact is not expired, has a non-zero size, and record its SHA-256 and expiration date.

### Task 4: Inspect the generated TIPA

**Files:**
- Downloaded Artifact: `DopamineAuto.tipa`

- [ ] **Step 1: Download and extract the Artifact.**
  Use `gh run download` for the exact run ID, copy the TIPA to ZIP, and extract `Payload/*.app`.

- [ ] **Step 2: Read `Info.plist`.**
  Confirm `CFBundleIdentifier` is `com.opa334.Dopamine-roothide`, display name is `DopamineAuto`, and `CFBundleExecutable` names an existing file.

- [ ] **Step 3: Report the final installable file and checks.**
  Provide the local path, file size, run URL, Artifact name, and the three inspected values in Chinese.
